@model IEnumerable<StoreFront.DATA.EF.Rarity>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RarityLevel)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RarityLevel)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RarityID }) |
            @Html.ActionLink("Details", "Details", new { id=item.RarityID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RarityID })
        </td>
    </tr>
}

</table>
